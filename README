Installation
------------
To build and install:

1. mkdir build
2. cd build
3. cmake ..
4. make
5. make install


I you have KDE4 installed, then cmake will detect this, and compile QtCurve's config dialog.
